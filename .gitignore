# Secrets — never commit real keys
.env
.env.local
*.key
# Google OAuth (for playlist auto-add) — these are SECRETS, keep them local only
client_secret*.json
token.json
*oauth*.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store
Thumbs.db

# Local Claude tooling / session data (never commit)
.claude/
