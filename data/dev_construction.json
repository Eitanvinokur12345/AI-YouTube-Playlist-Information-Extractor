{
  "updated_at": "2026-06-28T14:35:38.340309+00:00",
  "title": "Developer Construction — full rebuild specification",
  "intro": "A complete, precise description of every part of Excavatortron and exactly how it works, written so a developer with no prior context could rebuild the entire project from scratch. Read top to bottom. Each section is self-contained.",
  "sections": [
    {
      "id": "overview",
      "title": "1. What Excavatortron is",
      "body": "Excavatortron is a self-running, self-improving dashboard that mines a YouTube playlist of AI videos and turns everything it finds — techniques (skills), products (tools), AI models, MCP connectors, slash commands, prompts, tips, and news — into a clean, searchable web dashboard.\n\nIt runs entirely in the cloud on free infrastructure and improves itself on a schedule, with no owner intervention and no paid cost beyond a Claude Pro subscription the owner already has.\n\nThe single source playlist: PLxtsVIUtYhNo6pY9FrVqVe2xh---Y8rxB (~1,100 videos).\n\nData flow in one line:  YouTube playlist -> fetch (metadata+transcript) -> analyze (extract structured data) -> dashboard (render data/*.json).  Around that core sit self-improvement, quality review, web discovery, and news stages."
    },
    {
      "id": "prime-directive",
      "title": "2. Prime directive & hard constraints",
      "body": "PRIME DIRECTIVE: no owner intervention, and no new cost. 'Free' = public-repo GitHub Actions (unlimited free minutes) + the Claude Pro/Max SUBSCRIPTION token (not paid API billing) + free external API tiers that skip gracefully when absent.\n\nHARD CONSTRAINTS (every change must honor):\n- Never commit secrets. API keys live only in GitHub Actions secrets / local env vars; never printed, never committed.\n- Never modify a FROZEN/starred record (slug in data/stars.json, or starred/locked:true). Max 10 stars; only the owner stars.\n- Push automatically (the pipeline commits after every unit of work so a crash never loses progress).\n- Output is English only; the SOURCE transcript is never edited (used verbatim).\n- Never commit: make_icon.py, .claude/, OAuth client secrets (client_secret*.json), temp files. Stage explicit paths, never `git add -A`.\n- The owner's Claude plan is Pro ($20) = a small weekly token bucket. Heavy work is night-gated (Israel time) and offloaded to FREE engines wherever possible."
    },
    {
      "id": "architecture",
      "title": "3. Architecture (the cloud pipeline)",
      "body": "Everything is GitHub Actions workflows (.github/workflows/*.yml) on cron schedules, committing results back to the repo. The repo IS the database; the dashboard (GitHub Pages, served from repo root) reads ../data/*.json at runtime.\n\nSTAGES:\n  fetch.yml       -> pull new playlist videos + metadata -> data/_pending/<id>.json\n  transcribe.yml  -> caption backfill + Whisper ASR -> fills the transcript on pending records\n  analyze.yml     -> Claude (CLAUDE.md) extracts -> skills/tools/models/connectors/commands/tips/prompts\n  bulk_analyze.yml-> FREE multi-engine pool extracts the backlog (no Claude tokens)\n  news.yml        -> 50 web RSS sources -> news feeds + digest\n  improve.yml     -> weekly deep pass: dedup, calibrate, 50-question self-check, health\n  review.yml      -> 3-agent quality gate (usability / cut-the-bullshit / deep-code-bugs)\n  discover.yml    -> weekly web/GitHub/MCP tool discovery\n  sources.yml     -> suggest new channels when the playlist stops growing\n  codeql.yml      -> free static security scan\n  claude.yml      -> lets the owner drive changes by commenting on GitHub\n\nThe ENGINES are Markdown specs (CLAUDE.md, IMPROVE.md, REVIEW.md, DISCOVER.md) that Claude follows in the cloud. They — not the Python — are the live extraction/curation logic."
    },
    {
      "id": "transcripts",
      "title": "4. The transcript pipeline (the #1 hard problem)",
      "body": "ROOT INSIGHT: you cannot extract a technique from a video's description — you need the spoken transcript. Early on, 99.7% of videos had NO transcript because YouTube BLOCKS the caption API (youtube-transcript-api) from datacenter IPs (GitHub Actions). So the cloud 'read' videos from their description only -> shallow, boilerplate output.\n\nTWO-TIER TRANSCRIPT RECOVERY:\n  Tier 1 (captions, fast, text-only, free): src/backfill_transcripts.py fetches the YouTube caption track. RULE: accept a caption ONLY if it is a COMPLETE full-video transcript — auto-generated captions span the whole video; a manual track is accepted only if it covers >=90% of the video duration. Otherwise the video is left for Whisper.\n  Tier 2 (Whisper ASR, free, CPU): src/transcribe_local.py downloads the audio with yt-dlp and transcribes it with faster-whisper (tiny model, int8, no GPU). Runs in 'gaps' mode = only videos lacking an accepted complete caption.\n\nWHERE THEY RUN: transcribe.yml runs both in the cloud (caption backfill works from cloud IPs for MOST videos via the transcript API's player-client path; Whisper runs on the runner's CPU/disk). The local residential runner (sync/fetch-runner.ps1) is a fallback that recovers captions YouTube blocks in the cloud. Whisper uses ZERO Claude tokens.\n\nThe transcript is stored verbatim on the pending record (transcript_source = 'transcript' | 'whisper' | 'description' | 'title')."
    },
    {
      "id": "analysis",
      "title": "5. The two analysis lanes (Pro-budget design)",
      "body": "Deep-reading ~1,100 transcripts with the Claude Pro token would blow the small weekly limit. So there are TWO lanes:\n\nPREMIUM LANE (Claude, analyze.yml): follows CLAUDE.md exactly, highest quality, but NIGHT-GATED to the owner's Israel-time window (config.cadence.night_window 01:00-07:00) so it never drains his daytime quota. Reserved for incremental new videos + the improve/review curation.\n\nFREE BULK LANE (bulk_analyze.yml + src/bulk_analyze.py): drains the backlog and any future burst using a POOL of FREE engines — zero Claude tokens. It round-robins across every engine whose key is set (their daily quotas ADD UP):\n  - Claude Sonnet 4.6 via GitHub Models (= actual Claude quality, free, ~50/day)  [secret GH_MODELS_TOKEN]\n  - GPT-OSS 120B via Cerebras (near-frontier, free, ~1M tokens/day)               [secret CEREBRAS_API_KEY]\n  - Gemini 2.5 Flash (good, free, ~1,500/day)                                      [secret EXTERNAL_REVIEW_API_KEY]\nEach engine call sends the transcript + the CLAUDE.md extraction rules (anti-boilerplate, skills-vs-tools) and gets back strict JSON; deterministic Python then MERGES it into the data files (dedup by slug/name, union endorsement_video_ids).\n\nANTI-BOILERPLATE GATE (the #1 quality rule): never emit a skill that is a bare vendor name ('Claude', 'ChatGPT') or a template like 'Using X for productivity'. A skill must be a SPECIFIC method shown in the video. If a video only mentions a product, it becomes a TOOL, not a skill."
    },
    {
      "id": "skills-vs-tools",
      "title": "6. Skills vs Tools (the #1 content rule)",
      "body": "SKILL = a technique / workflow — something you DO (e.g. 'Claude Agent Loop', 'Nano-Banana image pipeline'). Lives in data/skills.json and a SKILL.md package folder.\nTOOL = a product / model / app / service — something that EXISTS (e.g. 'Cursor', 'Gemini 2.5 Pro', 'n8n'). Lives in data/tools.json.\nMODEL = a subset of tools (an AI model with a version) mirrored into data/models.json with a per-category podium.\nCONNECTOR = an MCP server / integration -> data/connectors.json.\n\nSKILL.md FOLDER ROUTING (one folder PER tool):\n  target_tool == 'claude'  -> skills/<slug>/SKILL.md      (syncs to Desktop 'claude skills of eitan')\n  any other tool           -> other-skills/<tool>/<slug>/SKILL.md  (syncs to '<tool> skills of eitan')\nThe sync (sync/sync-skills.ps1) also copies skills/ into ~/.claude/skills/ so every new Claude Code chat can load them."
    },
    {
      "id": "engines",
      "title": "7. The engine specs (the Markdown brains)",
      "body": "These Markdown files ARE the live logic; Claude reads and obeys them in the cloud.\n\nCLAUDE.md (analyze engine): Step 1 pick up data/_pending batch; Step 2 relevance gate; Step 2b video-quality score (1-10, recency penalty); Step 2c follow AI-relevant description links with WebFetch and mine them; Step 2d use the 'surroundings' (top comments, stats, tags); Step 2e comment-gated resources; Step 3 extract SKILLS; Step 3b extract TOOLS (extract EVERY named tool); Step 3c extract PROMPTS; Step 4 models ranking; Steps 5-8 improvement/tips/commands/news/connectors; commit + push after EACH video.\n\nIMPROVE.md (weekly deep pass): Step 0 load state + decide run mode (catch-up light / idle / full); Step 0c extraction-completeness (top priority — re-queue thin videos); Step 1/1b apply approved suggestions + self-check fix tasks; Step 2 data hygiene/dedup/index; Steps 3-7b near-dup, ratings, stars, UX self-review, skills-folder learning, dynamic tabs, feed health; Step 7c the 50-question reference self-check (ALWAYS runs); Step 8 health report; Step 9 audit.\n\nREVIEW.md (3-agent quality gate): Claude reviews first across usability (benchmarked vs competitors) + 'cut the bullshit' + deep code bugs; then src/external_review.py asks a DIFFERENT engine (Gemini) for a second opinion (graceful-skip); CodeQL is the automated deep-code arm.\n\nDISCOVER.md (web tool discovery): weekly, finds AI tools NOT already in tools.json from GitHub Trending + awesome-lists (incl. awesome-mcp-servers) + Product Hunt + web search; strong MCP/open-source emphasis; flags upcoming (unreleased) tools."
    },
    {
      "id": "workflows",
      "title": "8. The cloud workflows (.github/workflows)",
      "body": "Each is a cron + a job that commits results. Key wiring:\n- All use a concurrency group so two of the same never overlap.\n- Claude steps use anthropics/claude-code-action@v1 with claude_code_oauth_token = secrets.CLAUDE_CODE_OAUTH_TOKEN_REAL (the Pro subscription token).\n- The safety-net commit step runs `if: always()` and (critical fix) sets the git remote URL with secrets.GITHUB_TOKEN before pushing — otherwise the push fails with 'Invalid username or token'.\n- analyze.yml has a Plan step that night-gates heavy runs (zoneinfo Asia/Jerusalem) and respects catch-up mode.\n- transcribe.yml: Step 1 caption backfill (youtube-transcript-api) then Step 2 Whisper (faster-whisper). Stdlib + pip installs on the runner.\n- bulk_analyze.yml: stdlib only, passes the 3 engine secrets as env, round-robins the pool.\nSchedules (UTC): fetch frequent; transcribe every 4h; analyze every 3h (night-gated); bulk every 3h; news every 6h; improve Sat 20:00 + first-week nightly; review Sat 23:00 + first-week nightly; discover Sun 01:00; sources daily."
    },
    {
      "id": "scripts",
      "title": "9. The Python scripts (src/)",
      "body": "fetch.py — pulls playlist items via the YouTube Data API (YOUTUBE_API_KEY), the transcript (youtube-transcript-api), full description, resolved links, view/like/comment stats, tags, duration, and top comments; writes data/_pending/<id>.json.\nbackfill_transcripts.py — caption recovery with the COMPLETE-caption rule (auto-gen, or manual >=90% coverage); leaves the rest for Whisper. No API key needed.\ntranscribe_local.py — Whisper ASR (faster-whisper, tiny, int8) on yt-dlp audio; 'gaps' mode.\nbulk_analyze.py — the free multi-engine extraction pool (Gemini + OpenAI-compatible: GitHub Models / Cerebras); deterministic merge.\nnews.py — 50 RSS/Atom sources -> windowed feeds + a heuristic 'most important' digest.\nexternal_review.py — Gemini second-opinion for REVIEW.md (graceful-skip).\nrequeue.py — re-queue shallow/boilerplate videos for deep re-analysis.\nsuggest_channels.py — detect playlist-growth stall (<25/week) -> propose a high-value channel + 20 of its videos.\nadd_to_playlist.py + oauth_setup.py — one-time Google OAuth so approved channel videos are added to the playlist.\nmerge_dupes.py — dedup helper. (Dead/legacy: process_video.py, analyze_batch.py.)"
    },
    {
      "id": "data",
      "title": "10. The data files (data/)",
      "body": "CONTENT: skills.json {videos_seen[], skills[]}; tools.json {tools[]}; models.json {category: {podium,full_ranking}}; connectors.json; commands.json; tips.json {by_tool, general}; prompts.json {prompts[]}; dev_construction.json (this doc).\nNEWS: daily/weekly/monthly_news.json + _web variants; web_news_store.json; feeds_health.json. Each window file has header {window, covered_from/to, digest[]} + entries[].\nSTATE: status.json (run report, counters, analyze_ok, last_analyze); index.json (compact slug->score map); catch_up.json; _pending/ + processed/ (per-video work files); review_state.json (first-week anchor).\nSELF-IMPROVE/REVIEW: self_check.json (50 Q results + score); improvement_tasks.json; improvement_suggestions.json; approvals.json; improvement_audit.json; health.json; review_findings.json; tab_candidates.json + extra_tabs.json (dynamic tabs).\nDISCOVERY/SOURCES: discovery_log.json; channel_suggestions.json; source_growth.json; comment_gated.json.\nHOUSEKEEPING: deleted_skills.json (pre-delete snapshots); merge_log.json; stars.json (frozen records)."
    },
    {
      "id": "dashboard",
      "title": "11. The dashboard (docs/)",
      "body": "Vanilla JS, no build step. index.html (UI shell + CSS) + dashboard.js (renders every tab) + sw.js (service worker shell cache; bump SHELL_CACHE to force refresh) + manifest.webmanifest (PWA) + icon.png.\n\nCRITICAL: dashboard.js fetches ../data/*.json, so GitHub Pages MUST serve the WHOLE REPO from root (Settings -> Pages -> Deploy from a branch -> main -> / root). A root index.html redirects the bare URL to docs/.\n\nTABS: Skills Library, Tool Rating (merged tools+models, podium per category + 'what it does' + model filter), Coming Soon (release_status='upcoming'), Prompts (master/guardrail/creation, one-click copy), Improvement Log, Tips & Commands, News Feed (digest pinned on top), Connectors, Grow Sources (channel suggestions + comment-walled list), Self-Improvement (50-Q self-check + 3-agent review), Developer Construction (this). Plus a Quick-read toggle that condenses every tab, and auto-created Dynamic Tabs (NEW badge auto-expires)."
    },
    {
      "id": "selfimprove",
      "title": "12. The self-improvement loop",
      "body": "Each weekly deep pass (improve.yml) RETURNS TO THE OWNER'S REFERENCE SPEC (docs/REFERENCE_SPEC.md Part C = 50 yes/no questions) and answers all 50 against live data -> data/self_check.json {score, results[{n,question,answer,evidence}]}. Every 'no' opens a task in improvement_tasks.json {n,fix,kind: safe_auto|needs_approval|engine_followup}. Next run, Step 1b auto-applies the safe ones. The dashboard shows 'Self-check score X/50'.\n\nAutonomy is split (config.self_improvement.autonomy = safe_auto_suggest_risky): safe ops apply automatically; risky ops become suggestions in improvement_suggestions.json that the owner approves via the offline MCP server (mcp_server/server.py: approve_suggestion, approve_channel, star_skill, run_improve, ...) or by commenting on GitHub. Caps bound changes/run; a token budget keeps cost down; frozen records are never touched.\n\nHIGHER FIRST-WEEK ITERATION: improve + review run nightly for the first 7 days (anchored in review_state.json.first_run_at), then fall back to weekly."
    },
    {
      "id": "rebuild",
      "title": "13. Rebuild from scratch (step by step)",
      "body": "1. Create a PUBLIC GitHub repo (public = free unlimited Actions).\n2. Add the files: config.json, the 4 engine .md specs, src/*.py, .github/workflows/*.yml, docs/* (dashboard), data/ (seed empty/curated), brain/ (Obsidian vault).\n3. Add GitHub Actions SECRETS: YOUTUBE_API_KEY (fetch); CLAUDE_CODE_OAUTH_TOKEN_REAL (Pro token via `claude setup-token`, renew ~yearly); EXTERNAL_REVIEW_API_KEY (Gemini free); GH_MODELS_TOKEN (a GitHub PAT, free Claude Sonnet via GitHub Models); CEREBRAS_API_KEY (free); optional YOUTUBE_OAUTH_* (playlist auto-add).\n4. Enable GitHub Pages: Settings -> Pages -> Deploy from a branch -> main -> / (root). The dashboard serves at <user>.github.io/<repo>/docs (the root index.html redirects there).\n5. Set config.playlist_id to your playlist. The crons start on schedule; trigger any stage now with `gh workflow run <name>.yml` or the Actions 'Run workflow' button.\n6. (Optional, residential IP) Run sync/setup-sync.ps1 once to register the local fetch/transcribe runner + Desktop mirror.\nThat's the whole system: free, self-running, self-improving."
    },
    {
      "id": "gotchas",
      "title": "14. Gotchas & hard-won lessons",
      "body": "- YouTube blocks the caption API AND throttles audio from datacenter IPs -> use the complete-caption rule + Whisper + a residential fallback.\n- The safety-net `git push` in a workflow fails ('Invalid username or token') unless you reset the remote URL with secrets.GITHUB_TOKEN first.\n- GitHub reserves the GITHUB_ secret-name prefix -> use GH_MODELS_TOKEN.\n- GitHub Pages must serve the REPO ROOT (dashboard reads ../data), not /docs.\n- Google AI Studio (Gemini) requires the account to be 18+/verified; GitHub Models + Cerebras do not.\n- On a Pro plan the 5 Claude workflows fight one small weekly bucket -> night-gate analyze, offload bulk to free engines, keep improve/review weekly.\n- Anti-boilerplate gate is essential: without it, dedup collapses ~950 'Claude/ChatGPT' vendor stubs and the library looks empty.\n- Never auto-comment on YouTube to harvest gated files (ToS violation -> account ban, and the files usually arrive by DM anyway). Resolve gated resources by searching the creator's PUBLIC footprint + web instead."
    },
    {
      "id": "brain",
      "title": "15. The Obsidian brain (knowledge graph)",
      "body": "src/build_brain.py turns the WHOLE project into a rich Obsidian vault so it can be browsed and SEEN as a graph in the desktop Obsidian app.\n\nWHAT IT MAKES: one note per skill / tool / prompt / connector, each wikilinked to its CATEGORY hub and (for skills) its TOOL hub, plus a Connectors hub, the 25 structural 'how it's built' notes (copied into Project/), and a Home page. ~573 notes, verified 0 ORPHANS — the graph is one fully-connected web that clusters by category and tool.\n\nWHERE / HOW TO VIEW: written to the owner's local vault at OneDrive/Documents/Excavatortron obsidian brain/Excavatortorn. Open that folder as a vault in Obsidian, open Home.md, then open the Graph view. (The web dashboard can't render Obsidian's live graph — the brain is viewed inside Obsidian.) Regenerate any time: python -m src.build_brain \"<vault path>\".\n\nRULE: keep the graph fully connected — any new note type must get a hub link so nothing becomes an orphan. NOTE: this is separate from the repo's brain/ folder, which holds only the 25 structural notes that build_brain copies into Project/."
    },
    {
      "title": "13. Transcript recovery — why the cloud can't, and the residential model",
      "body": "THE core bottleneck. YouTube blocks BOTH caption fetches (youtube-transcript-api -> the timedtext endpoint) AND audio downloads (yt-dlp -f bestaudio) from datacenter IPs. GitHub Actions runs on Azure, so transcribe.yml returns 0 every run ('backfilled 0 ... 400 no caption', 'whispered 0, skipped 60') — verified in the Actions logs. It is NOT throttling; it is a hard IP ban, so parallelism cannot help. transcribe.yml is therefore cut to a daily safety-net.\n\nThe ONLY working lever is src/backfill_transcripts.py run from a RESIDENTIAL IP (Eitan's PC). A random sample shows ~85-88% of the 1,112 caption-less videos DO have an English caption fetchable from home. The script: (1) finds processed records lacking transcript_source in (transcript, whisper); (2) fetches the auto/manual en|he caption, else TRANSLATES a foreign auto-caption to English; (3) writes the record to data/_pending for the free analysis lane. YouTube's rate-limit ESCALATES on bursts, so the script raises RateLimited on IpBlocked/TooManyRequests and STOPS the run rather than deepening the block; pace with --sleep >=1.5 and never burst. Because Eitan's PC can't stay on, the model is: drain during sessions (cloud analyzes 24/7). Unattended PC-free fallback: src/supadata_fetch.py (Step 0 of transcribe.yml) calls Supadata's free tier (~100/mo, their infra, AI-fallback for no-caption videos), graceful-skip without SUPADATA_API_KEY."
    },
    {
      "title": "14. Effectiveness scoreboard (src/effectiveness.py)",
      "body": "Measures every retrieval/analysis LANE so the self-improvement system has concrete targets (data/effectiveness.json, recomputed each analysis cycle via bulk_analyze.yml; stdlib only). Each lane scores 0-10 on 8 dimensions: quality, quantity, form, time, tokens, ease_external, ease_project, ease_user (weights in WEIGHTS). 'effectiveness' = weighted mean; 'rigidity' = brittleness/lock-in (higher worse, e.g. the IP-blocked cloud lane = 10). Live signals from health.json (coverage), skills/tools.json (avg quality, low-source fraction) and the transcript-source mix overlay a documented per-lane baseline; the weekly Claude pass refines the rated dims. Lanes: transcript_residential/cloud/supadata, analysis_free/claude, web_news, tool_discovery, source_suggestion. IMPROVE.md Step 6c (Module 10) reads it and opens improvement_tasks for the weakest lanes' weak_dims; the recurring weak dim 'ease_external' drives the north-star task: build a machine-readable hub index/API for external systems. Rendered on the dashboard's Effectiveness tab (weakest-first)."
    },
    {
      "title": "15. Security model & data-privacy",
      "body": "The repo is PUBLIC (free Actions). Cloud workflows run on GitHub runners scoped to the repo only — they have ZERO access to Eitan's PC or files. The crown-jewel secret is CLAUDE_CODE_OAUTH_TOKEN_REAL (the Pro subscription token); it plus the API keys live ONLY in GitHub Actions secrets (encrypted), never in code/commits/logs. The main attack surface is claude.yml (@claude from issues/PRs): it now requires BOTH '@claude' AND a trusted author_association (OWNER/MEMBER/COLLABORATOR), so a stranger's comment can't even start a runner (defense-in-depth on top of claude-code-action's own write-access gate). Scheduled workflows are least-privilege (contents:write; codeql is contents:read). Data sent to the free LLM engines and Supadata is PUBLIC YouTube content (transcripts/metadata) — no PII. Local scripts (sync/*.ps1, mcp_server/server.py) run only when Eitan launches them, scoped to data/. The self-improvement loop now re-audits all of this every run (IMPROVE.md Module 12 + REVIEW.md security_and_privacy dimension + CodeQL)."
    },
    {
      "title": "A. Workflows & schedules (auto)",
      "body": "The cloud pipeline = these GitHub Actions workflows (all free on the public repo). Schedules are UTC.\n\n- **Analyze pending** (`analyze.yml`) — triggers: schedule, workflow_dispatch; cron: 0 */3 * * *; */30 * * * *\n- **Bulk analyze (free, Gemini)** (`bulk_analyze.yml`) — triggers: schedule, workflow_dispatch; cron: 30 */3 * * *\n- **Claude (interactive, @claude)** (`claude.yml`) — triggers: issue_comment, issues; cron: —\n- **CodeQL (deep code scan)** (`codeql.yml`) — triggers: schedule, workflow_dispatch, push; cron: 0 22 * * 6\n- **Discover tools** (`discover.yml`) — triggers: schedule, workflow_dispatch; cron: 0 1 * * 0,2,4\n- **Engine self-test (free pool)** (`engine_selftest.yml`) — triggers: workflow_dispatch; cron: —\n- **Fetch playlist** (`fetch.yml`) — triggers: schedule, workflow_dispatch; cron: 0 6 */2 * *\n- **Gemini video analysis (watch videos directly — no transcript)** (`gemini_video.yml`) — triggers: schedule, workflow_dispatch; cron: 0 5 * * *; 0 11 * * *; 0 17 * * *; 0 23 * * *\n- **Self-improve** (`improve.yml`) — triggers: schedule, workflow_dispatch; cron: 0 20 * * 6; 0 20 * * 0-5\n- **Mine feeds (external → skills/tools/MCP)** (`mine.yml`) — triggers: schedule, workflow_dispatch; cron: 0 10 * * *\n- **Fetch web news** (`news.yml`) — triggers: schedule, workflow_dispatch; cron: 0 */6 * * *\n- **Review (3 agents)** (`review.yml`) — triggers: schedule, workflow_dispatch; cron: 0 23 * * 3,6; 0 23 * * 0-2,4-5\n- **Suggest sources** (`sources.yml`) — triggers: schedule, workflow_dispatch; cron: 0 6 * * *\n- **Transcribe (cloud, Supadata — unattended)** (`transcribe.yml`) — triggers: schedule, workflow_dispatch; cron: 0 4 * * *"
    },
    {
      "title": "B. Python modules (auto)",
      "body": "Every Python module in `src/` and its one-line purpose:\n\n- `src/add_to_playlist.py` — src/add_to_playlist.py — insert APPROVED suggested videos into the real YouTube playlist (OAuth).\n- `src/analyze_batch.py` — Batch analyzer for YouTube AI Skills Tracker.\n- `src/backfill_transcripts.py` — src/backfill_transcripts.py — recover REAL transcripts from a RESIDENTIAL IP.\n- `src/backup_system.py` — src/backup_system.py — daily backup + REGRESSION GUARD, so nothing is silently lost.\n- `src/build_brain.py` — src/build_brain.py — generate a rich Obsidian \"brain\" of the WHOLE project from the data, so\n- `src/build_dev_doc.py` — src/build_dev_doc.py — keep the dashboard's Dev Construction tab TECHNICAL and CURRENT.\n- `src/build_graph.py` — src/build_graph.py — emit data/brain_graph.json (nodes + links) for the dashboard's in-page\n- `src/build_hub_index.py` — src/build_hub_index.py — the PUBLIC HUB INDEX (the north-star \"hub for future systems\").\n- `src/build_models.py` — src/build_models.py — mirror the AI MODELS out of tools.json into data/models.json.\n- `src/build_pipeline_graph.py` — src/build_pipeline_graph.py — the SECOND brain: the system/protocol orchestration graph.\n- `src/bulk_analyze.py` — src/bulk_analyze.py — FREE bulk analyzer (the two-tier \"bulk lane\").\n- `src/cross_tab_check.py` — src/cross_tab_check.py — the single-tab guarantee (cross-tab consistency).\n- `src/effectiveness.py` — src/effectiveness.py — the EFFECTIVENESS & RIGIDITY scoreboard (Eitan's request).\n- `src/export_graphml.py` — src/export_graphml.py — export the WHOLE brain as GraphML for Graphify / Gephi / Neo4j / yEd.\n- `src/external_review.py` — src/external_review.py  —  External \"second opinion\" for the review stage.\n- `src/fetch.py` — src/fetch.py  —  YouTube playlist fetch + transcript pull + news classification.\n- `src/gemini_video_analyze.py` — src/gemini_video_analyze.py — analyze videos DIRECTLY (audio + VISUAL) via Gemini's YouTube-URL input.\n- `src/health.py` — src/health.py — a TINY, mechanical progress readout (Phase 1). NO Claude, NO network,\n- `src/improve_log.py` — src/improve_log.py — a short, human one-liner after each self-improvement cycle.\n- `src/maintenance_check.py` — src/maintenance_check.py — the MAINTENANCE protocol (part of self-improvement).\n- `src/merge_dupes.py` — Merge duplicate skill slugs caused by the make_slug bug.\n- `src/mine_feeds.py` — src/mine_feeds.py — MINING SYSTEM (separate from self-improvement).\n- `src/news.py` — src/news.py  —  Daily AI news from official web sources (RSS/Atom).\n- `src/news_digest.py` — src/news_digest.py — the system's OWN editorial summary of the AI news (not raw headlines).\n- `src/oauth_setup.py` — src/oauth_setup.py — ONE-TIME local helper to let Excavatortron edit your YouTube playlist.\n- `src/pipeline_status.py` — src/pipeline_status.py — a LIVE heartbeat readout of the whole retrieval+analysis pipeline.\n- `src/priorities.py` — src/priorities.py — DYNAMIC PRIORITIES: keep the dashboard focused on what matters NOW.\n- `src/process_video.py` — Helper script for processing a single pending video.\n- `src/recategorize.py` — src/recategorize.py — fix mis-categorized items (e.g. robots filed as generic 'tool'/'agents').\n- `src/requeue.py` — src/requeue.py — re-queue shallow/boilerplate videos for DEEP re-analysis.\n- `src/resolve_comment_gated.py` — src/resolve_comment_gated.py — recover resources the creator gated behind \"comment / link below\".\n- `src/resolve_links.py` — src/resolve_links.py — THE ACCESS protocol: give every tool/skill/connector REAL, working links.\n- `src/safety_check.py` — src/safety_check.py — mechanical SAFETY rating for connectors (and a light pass on skills).\n- `src/self_check.py` — src/self_check.py — the 50-question reference self-check, run MECHANICALLY (no Claude).\n- `src/source_bundles.py` — src/source_bundles.py — give every item a SOURCE-VIDEO BUNDLE, earliest-first.\n- `src/suggest_channels.py` — src/suggest_channels.py — grow the playlist when it stalls.\n- `src/supadata_fetch.py` — src/supadata_fetch.py — UNATTENDED, PC-free transcript recovery via Supadata's free tier.\n- `src/token_startup.py` — src/token_startup.py — the TOKEN-REDUCTION protocol: run ALL token reducers on every startup.\n- `src/token_tips.py` — src/token_tips.py — surface the hub's best TOKEN-EFFICIENCY skills for Claude.\n- `src/transcribe_local.py` — src/transcribe_local.py — local Whisper ASR (free, CPU-only, no GPU).\n- `src/trend_watch.py` — src/trend_watch.py — the TREND-IDENTIFICATION protocol (part of self-improvement)."
    },
    {
      "title": "C. Data files & schemas (auto)",
      "body": "The committed JSON the dashboard + external systems read (see HUB_API.md for the public manifest):\n\n- `data/_removed_cross_tab.json` — `{removed: [13]}`; item fields: tab, name, slug, source_url\n- `data/agent_catalog.json` — `{endpoints: [20]}`; item fields: file, url, description, top_level_keys, skills_record_fields\n- `data/backup_status.json` — `{regressions: [3]}`; item fields: type, was, now, dropped, missing_examples\n- `data/brain_graph.json` — `{links: [341]}`; item fields: source, target\n- `data/catch_up.json` — object, keys: active, mode, reason, surge_threshold, batch_size, order, last_new_count, last_pending\n- `data/channel_suggestions.json` — `{suggestions: [1]}`; item fields: channel, channel_id, channel_title, reason, status, generated_at, videos\n- `data/commands.json` — `{commands: [123]}`; item fields: command, description, tool, source_video\n- `data/comment_gated.json` — `{items: [23]}`; item fields: video_id, title, source_url, gate_phrase, comment_keyword, what_it_is, added_at, status, resolved_url, resolution_note\n- `data/comment_resolved.json` — `{video_ids: [64]}`\n- `data/connectors.json` — `{connectors: [266]}`; item fields: name, type, provider, category, what_it_does, install_or_source, official, quality_score, source, url\n- `data/cross_tab_conflicts.json` — `{conflicts: [0]}`\n- `data/daily_news.json` — `{entries: [6]}`; item fields: video_id, title, channel_name, publishedAt, summary\n- `data/daily_web_news.json` — `{entries: [225]}`; item fields: url, title, source_name, publishedAt, summary, source_type\n- `data/deleted_skills.json` — array of 1065\n- `data/dev_construction.json` — `{sections: [21]}`; item fields: id, title, body\n- `data/discovery_log.json` — `{runs: [5]}`; item fields: ran_at, added, connectors_added, scanned_sources, notable\n- `data/effectiveness.json` — `{dimensions: [8]}`\n- `data/extra_tabs.json` — `{tabs: [0]}`\n- `data/feeds_health.json` — object, keys: generated_at, feeds\n- `data/gemini_analyzed.json` — `{visual_notes: [457]}`; item fields: video_id, note, at\n- `data/health.json` — object, keys: generated_at, videos, library\n- `data/hub.json` — `{datasets: [11]}`; item fields: id, file, url, root_key, count, fields, description\n- `data/improve_log.json` — `{entries: [30]}`; item fields: at, text\n- `data/improvement_suggestions.json` — `{suggestions: [21]}`; item fields: id, type, status, detail, proposed_change, source, review_finding_id, severity\n- `data/improvement_tasks.json` — `{tasks: [17]}`; item fields: trend_key, kind, question, fix, goal, status, created_at\n- `data/index.json` — object, keys: claude-code, deepseek, chatgpt, claude, gemini, manus, cursor, make\n- `data/links_state.json` — `{done: [840]}`\n- `data/maintenance.json` — `{issues: [1]}`; item fields: severity, area, issue, count, fix, sample\n- `data/merge_log.json` — array of 1060\n- `data/mined_state.json` — `{urls: [672]}`\n- `data/models.json` — `{models: [0]}`\n- `data/monthly_news.json` — `{entries: [825]}`; item fields: video_id, title, channel_name, publishedAt, summary\n- `data/monthly_web_news.json` — `{entries: [2676]}`; item fields: url, title, source_name, publishedAt, summary, source_type\n- `data/news_digest.json` — object, keys: generated_at, windows\n- `data/pipeline_graph.json` — `{links: [57]}`; item fields: source, target\n- `data/pipeline_status.json` — `{history: [59]}`; item fields: at, counts\n- `data/priorities.json` — `{priorities: [6]}`; item fields: rank, impact, area, title, detail\n- `data/prompts.json` — `{prompts: [225]}`; item fields: title, category, purpose, prompt_text, source, notes, curated\n- `data/review_findings.json` — `{findings: [29]}`; item fields: id, dimension, severity, area, where, detail, suggestion, status\n- `data/review_state.json` — object, keys: first_run_at\n- `data/safety.json` — object, keys: generated_at, policy, summary, connectors, skills_flagged\n- `data/self_check.json` — `{results: [50]}`; item fields: n, question, answer, evidence\n- `data/skills.json` — `{skills: [0]}`\n- `data/source_growth.json` — `{history: [20]}`; item fields: ts, total_seen\n- `data/status.json` — object, keys: analyze_ok, token_hint, last_run, last_fetch, next_run, videos_seen, total_skills, total_videos_analyzed\n- `data/tab_candidates.json` — `{candidates: [18]}`; item fields: theme, label, note, video_id, source_url, ts\n- `data/tips.json` — object, keys: by_tool, general\n- `data/token_active.json` — `{core_reducers: [8]}`; item fields: name, technique\n- `data/token_tips.json` — `{commands: [5]}`; item fields: command, description\n- `data/tools.json` — `{tools: [0]}`\n- `data/trends.json` — `{history: [30]}`; item fields: at, total, categories, topics\n- `data/web_news_store.json` — `{items: [4441]}`; item fields: url, title, source_name, publishedAt, summary, source_type, first_seen\n- `data/weekly_news.json` — `{entries: [175]}`; item fields: video_id, title, channel_name, publishedAt, summary\n- `data/weekly_web_news.json` — `{entries: [1540]}`; item fields: url, title, source_name, publishedAt, summary, source_type"
    }
  ]
}