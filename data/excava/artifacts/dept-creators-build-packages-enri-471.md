# creators: Build PACKAGES + enrich tabs + draft elements; every one labeled 'Created by EXCAVA' + tes

> Decision artifact · room `dept-creators-build-packages-enri-471` (dept) · 2026-07-10T10:04:41.979962+00:00
> Participants: Forge, Vet, Nova · synthesized by gh-models/openai/gpt-4o-mini

**Decision:** Forge a consensus schema (`tabs/schema.toml`) after collaborative validation.  

**Plan:**  
1. Schedule a 10-minute whiteboard session with Vet, Forge, and Lead to co-validate the fields for the `tabs/schema.toml`.  
2. Define the required fields in the schema as: `name` (string), `type` (enum: [static, dynamic, hybrid]), and `required_fields` (array).  
3. Once validated, draft the `tabs/schema.toml` file incorporating the consensus from the session.  
4. After the schema is locked, create 3 starter tab files in `tabs/` labeled `Created by EXCAVA`.  
5. Share the finalized schema and starter tabs with the broader team for feedback.  

**What changed:** The decision shifted from prematurely seeding tab files to prioritizing schema validation through collaborative consensus.
