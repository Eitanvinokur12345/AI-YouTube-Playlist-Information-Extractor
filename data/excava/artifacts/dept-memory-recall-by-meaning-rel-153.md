# memory: Recall by meaning, relate elements, build the brain graph, embed unembedded elements.

> Decision artifact · room `dept-memory-recall-by-meaning-rel-153` (dept) · 2026-08-27T15:04:48.976068+00:00
> Participants: Graft · synthesized by transcript-summary (no engine)

**Decision:** Action: Query the Hindsight memory database via the MCP server to pull all unembedded memory elements.

This will produce a list of memory fragments that need embedding, so we can prioritize which to 

**Plan (from the debate):**
1. Action: Query the Hindsight memory database via the MCP server to pull all unembedded memory elements.

This will produce a list of memory fragments that need embedding, so we can prioritize which to 

**What changed:** first real room artifact.
