# visual: MINE great designs FROM THE WEB into the hub — screenshots, taste material, design pattern

> Decision artifact · room `dept-visual-mine-great-designs-fr-123` (dept) · 2026-08-30T03:39:03.183766+00:00
> Participants: Easel, Squint, Chroma · synthesized by mistral/mistral-small-latest

**Decision:**

**Plan:**
1. Open the official Model Context Protocol (MCP) website in a browser.
2. Capture a full-page screenshot of the MCP homepage.
3. Save the screenshot to the hub’s `/design-patterns/` directory with a filename like `mcp-homepage-YYYYMMDD.png`.
4. Add a brief description in the hub’s `README.md` linking the screenshot to MCP’s design language.
5. Tag the file with `#mcp`, `#design-pattern`, and `#brand-reference` in the hub’s metadata.
6. Notify the team via the hub’s `#updates` channel with the screenshot’s location.

**What changed:** Added MCP homepage screenshot to `/design-patterns/` as a clean visual reference.
