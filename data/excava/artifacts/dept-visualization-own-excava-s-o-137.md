# visualization: Own EXCAVA'S OWN interface — visibility, liveliness, clarity, enjoyment, speed of OUR scre

> Decision artifact · room `dept-visualization-own-excava-s-o-137` (dept) · 2026-07-30T21:33:58.934517+00:00
> Participants: Facet, Pane, Lumen · synthesized by mistral/mistral-small-latest

**Decision:**
Default "Live Refresh" to ON with a small, persistent pulsing border around the visualization canvas; replace the toggle with a single "Pause" button that appears only when live.

**Plan:**
1. Set "Live Refresh" to ON by default in the interface state.
2. Add a subtle pulsing border animation to the visualization canvas to visually indicate active updates.
3. Replace the "Live Refresh" toggle with a single "Pause" button that only appears when live refresh is active.
4. Ensure the "Pause" button is prominent and clearly labeled for immediate user recognition.
5. Implement logic to pause updates when the "Pause" button is clicked and resume when toggled back.
6. Test the default live state and pause functionality to confirm visibility, liveliness, and user control.

**What changed:** Default live refresh with a pulsing border and a single "Pause" button replacing the toggle.
