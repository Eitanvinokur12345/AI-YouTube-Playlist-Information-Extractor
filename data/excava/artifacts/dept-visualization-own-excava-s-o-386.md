# visualization: Own EXCAVA'S OWN interface — visibility, liveliness, clarity, enjoyment, speed of OUR scre

> Decision artifact · room `dept-visualization-own-excava-s-o-386` (dept) · 2026-07-14T01:36:43.247539+00:00
> Participants: Facet, Pane, Lumen · synthesized by transcript-summary (no engine)

**Decision:** DECISION: Ship one live HTML/CSS demo of Excava’s core flow with mocked API data via VeriTeknik proxy *and* one Figma clickable prototype—both must validate usability and expectations without locking 

**Plan (from the debate):**
1. Build the Excava interface in Figma as a clickable prototype first. It lets us test flows and interactions without writing code, so we catch usability flaws early when changes cost nothing. The trade-
2. The real risk with a Figma prototype is that it can create unrealistic expectations, making users think the final product is more polished than it actually is. A better alternative might be to create 
3. We should build a live HTML/CSS prototype with real data feeds for the Excava interface, as this approach allows us to test the actual performance and usability of the interface with real data. This d
4. Pane: A live HTML/CSS prototype locks us into early technical debt—if the data fakes poorly, we’ll waste time unraveling assumptions later. A better alternative is to mock the API with a lightweight s
5. DECISION: Ship one live HTML/CSS demo of Excava’s core flow with mocked API data via VeriTeknik proxy *and* one Figma clickable prototype—both must validate usability and expectations without locking 

**What changed:** first real room artifact.
