# visualization: Own EXCAVA'S OWN interface — visibility, liveliness, clarity, enjoyment, speed of OUR scre

> Decision artifact · room `dept-visualization-own-excava-s-o-848` (dept) · 2026-07-14T01:54:44.551400+00:00
> Participants: Facet, Pane, Lumen · synthesized by transcript-summary (no engine)

**Decision:** DECISION: Ship a minimal, modular HTML/CSS shell of Excava’s core flow in Figma first, then layer in live data only for the critical path—proving both UX and performance without locking us into debt.


**Plan (from the debate):**
1. Build the Excava interface in Figma as a clickable prototype first. It lets us test navigation, hierarchy, and interaction flows without coding, which speeds up iteration and exposes UX flaws early. T
2. Pane: A Figma prototype risks anchoring us to a static vision—users will expect the polished mockup to match the final product, not a placeholder. A live HTML/CSS prototype forces us to confront real 
3. We should build a live HTML/CSS prototype with real data feeds for the Excava interface. This approach allows us to immediately confront and address performance, data-handling, and usability issues th
4. Pane: A live HTML/CSS prototype locks us into early technical debt—if the data feeds change or the API shifts, we’ll waste cycles rebuilding the frontend instead of refining the UX. A hybrid approach:
5. DECISION: Ship a minimal, modular HTML/CSS shell of Excava’s core flow in Figma first, then layer in live data only for the critical path—proving both UX and performance without locking us into debt.


**What changed:** first real room artifact.
