# visualization: Own EXCAVA'S OWN interface — visibility, liveliness, clarity, enjoyment, speed of OUR scre

> Decision artifact · room `dept-visualization-own-excava-s-o-983` (dept) · 2026-08-25T22:59:06.616069+00:00
> Participants: Facet, Pane, Lumen · synthesized by transcript-summary (no engine)

**Decision:** DECISION: Embed the legend as a semi-transparent overlay that collapses into a compact icon when space tightens, expanding on hover or tap—with a persistent minimalist bar as fallback if the icon is m

**Plan (from the debate):**
1. Make the legend a sticky panel anchored to the left edge of the graph, always visible. This keeps the meaning of colors and symbols in the user’s field of view without forcing them to hunt for a toolt
2. Pane: A sticky legend still risks crowding the graph when users zoom into dense data clusters—controls vanish under the legend’s shadow. Better: a floating legend that collapses into a compact icon wh
3. Choose a floating legend that collapses into a compact icon when the user zooms into dense clusters. This keeps controls visible without permanently shrinking the graph, solving the crowding problem w
4. Pane: A floating legend that collapses on zoom still risks users missing the icon in a cluttered view—what if the collapse state blends into the graph’s noise? Better: a persistent, minimalist legend 
5. DECISION: Embed the legend as a semi-transparent overlay that collapses into a compact icon when space tightens, expanding on hover or tap—with a persistent minimalist bar as fallback if the icon is m

**What changed:** first real room artifact.
