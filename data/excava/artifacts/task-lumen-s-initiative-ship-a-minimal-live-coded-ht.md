# [Lumen's initiative] Ship a minimal, live-coded HTML/CSS demo of Excava’s core flow—no Figma, no polish—so users can *break* it in real time 

> visualization · task `lumen-s-initiative-ship--29033` · **EXECUTION PLAN — NOT yet executed** · by groq/llama-3.3-70b-versatile

**Approach:** Create a minimal HTML/CSS demo for Excava's core flow using live coding, focusing on simplicity and readability.
**Steps:**
1. Initialize a new Git repository using `git init` and create a basic directory structure with `mkdir excava-demo` and `cd excava-demo`.
2. Create an `index.html` file using `touch index.html` and add basic HTML structure, then create a `style.css` file using `touch style.css` and link it to the HTML file.
3. Use a live coding tool like `live-server` (install with `npm install -g live-server`) to serve the demo, running `live-server` in the terminal to enable real-time updates and user testing.
**Needs:**
* A code editor (e.g., Visual Studio Code)
* Git version control system
* Node.js and npm for live-server installation
* A terminal or command prompt for execution
* Basic knowledge of HTML, CSS, and live coding principles
