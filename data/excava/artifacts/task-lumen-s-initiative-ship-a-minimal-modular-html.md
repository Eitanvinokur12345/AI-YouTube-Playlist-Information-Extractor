# [Lumen's initiative] Ship a minimal, modular HTML/CSS shell of Excava’s core flow in Figma first, then layer in live data only for the critic

> visualization · task `lumen-s-initiative-ship--94084` · **EXECUTION PLAN — NOT yet executed** · by groq/llama-3.3-70b-versatile

**Approach:** Create a minimal, modular HTML/CSS shell of Excava's core flow in Figma and then integrate live data.
**Steps:**
1. Design the Excava core flow wireframe in Figma, using frames and components to create a modular structure, and export the design as HTML and CSS files.
2. Set up a local development environment with a code editor (e.g., Visual Studio Code) and create a new folder for the project, then add the exported HTML and CSS files to the folder.
3. Use a task runner (e.g., Node.js with npm) to install required dependencies (e.g., a CSS preprocessor like Sass) and create a build process to compile and serve the HTML/CSS files, integrating live data for the critic via API calls or data ingestion tools.
**Needs:**
* Figma account and access to the Excava design file
* Code editor (e.g., Visual Studio Code)
* Node.js and npm installed on the local machine
* API access or data ingestion tools for integrating live data
* Basic knowledge of HTML, CSS, and JavaScript
