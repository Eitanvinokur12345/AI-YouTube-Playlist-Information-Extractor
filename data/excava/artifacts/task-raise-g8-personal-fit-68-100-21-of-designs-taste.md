# Raise G8 Personal fit (68/100): 21% of designs taste-tagged; Arena learning live; NOSG wired (next: taste beyond

> visual · task `raise-g8-personal-fit-68-83485` · **EXECUTION PLAN — NOT yet executed** · by mistral/mistral-small-latest

```markdown
**Approach:** Audit taste-tag coverage, automate tagging via Arena learning, and expand taste validation beyond current scope.

**Steps:**
1. **Audit:** Run `scripts/audit_taste_tags.py` (existing) to generate `data/taste_coverage_report.json` with un-tagged designs.
2. **Tag:** Use `tools/arena_learner.py --mode live --input data/taste_coverage_report.json --output data/taste_tags_updated.json` to auto-tag 21%+ of designs.
3. **Validate:** Manually review `data/taste_tags_updated.json` in `scripts/validate_tags.py` (interactive CLI) and export `validated_tags.csv`.
4. **Integrate:** Merge `validated_tags.csv` into `data/designs_metadata.json` via `tools/update_metadata.py --file designs_metadata.json --tags validated_tags.csv`.
5. **Expand:** Run `scripts/expand_taste_beyond.py --source designs_metadata.json --target designs_taste_extended.json` to push taste validation beyond current scope.

**Needs:**
- `scripts/audit_taste_tags.py` (existing)
- `tools/arena_learner.py` (existing, `--mode live` flag required)
- `data/taste_coverage_report.json` (generated by Step 1)
- `scripts/validate_tags.py` (existing interactive CLI)
- `tools/update_metadata.py` (existing)
- `scripts/expand_taste_beyond.py` (existing)
```
